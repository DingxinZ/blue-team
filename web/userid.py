#-----------------------------------------------------------------------------
# This class loads html files from the "template" directory and formats them using Python.
# You can find a fuller explanation for this file in the README file
#-----------------------------------------------------------------------------

class userid():

    def __init__(self, logged_in = False, email_address = None, isadmin = False, username = "admin", userid = 0, userheader = "header"):
        self.logged_in = logged_in
        self.email_address = email_address
        self.isadmin = isadmin
        self.username = username
        self.userid = userid
        self.userheader = userheader
